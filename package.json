{
  "name": "carreteo",
  "version": "2.0.0",
  "private": true,
  "description": "Juego de fiesta en un sitio estático. No necesita build: index.html se abre tal cual.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "smoke": "node tools/smoke.js .",
    "check": "npm test && npm run smoke",
    "serve": "python -m http.server 8080"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
